import { environment } from '../environments/environment';

export const applicationUrls = {
  readJson: {
    read: environment.baseApiUrl + 'Home',
    save: environment.baseApiUrl + 'Home',
  },
};
